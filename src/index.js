import React from "react"
import ReactDOM from "react-dom/client"
import App from "./app"
import MemeGene from "./memeGenerator"
import Upload from "./Upload"
const root = ReactDOM.createRoot(document.getElementById('root'))

root.render(
    // <MemeGene/>
    <Upload/>

)